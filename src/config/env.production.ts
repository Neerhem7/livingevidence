import { EnvironmentConfig } from './types';

export const config: EnvironmentConfig = {
  API_URL: 'https://app.lisr.org/backend', // Replace with your actual production API URL
  ENV: 'production'
}; 